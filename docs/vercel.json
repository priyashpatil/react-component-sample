{
  "buildCommand": "cd ../core && npm install && npm run build && cd ../docs && npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}